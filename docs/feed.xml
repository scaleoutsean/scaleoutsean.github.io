<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en_US"><generator uri="https://jekyllrb.com/" version="4.1.1">Jekyll</generator><link href="https://scaleoutsean.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://scaleoutsean.github.io/" rel="alternate" type="text/html" hreflang="en_US" /><updated>2020-12-05T16:12:53+08:00</updated><id>https://scaleoutsean.github.io/feed.xml</id><title type="html">Acting Technologist</title><subtitle>human action through technology</subtitle><author><name>scaleoutSean</name></author><entry><title type="html">How much slower is iSCSI vs. Fibre Channel</title><link href="https://scaleoutsean.github.io/2020/12/05/iscsi-vs-fibre-channel-fc-performance.html" rel="alternate" type="text/html" title="How much slower is iSCSI vs. Fibre Channel" /><published>2020-12-05T00:00:00+08:00</published><updated>2020-12-05T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/12/05/iscsi-vs-fibre-channel-fc-performance</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/12/05/iscsi-vs-fibre-channel-fc-performance.html">&lt;p&gt;If you search for “iSCSI vs FC performance” it’s not easy to find a recent like-for-like comparison from enterprise vendors. I’ve been puzzled by this and I have some theories as to why, but regardless - the fact is there aren’t that many comparisons out there.&lt;/p&gt;

&lt;p&gt;Last time I looked really hard for such material from NetApp I found an old PDF with a comparison for ONTAP systems (FAS, at the time, if I recall correctly). I have this link in the Awesome SolidFire repository, but to save you time, FC was slightly faster (single digit percentage figure.) There’s a nice &lt;a href=&quot;https://www.snia.org/sites/default/files/ESF/FC_vs_iSCSI_Jan2018_Final.pdf&quot;&gt;presentation&lt;/a&gt; with (only) protocol comparison over at SNIA, but it’s vendor- and hardware-neutral, so you won’t find specific examples for servers and storage you can buy today.&lt;/p&gt;

&lt;p&gt;If you have an environment that supports or can support both FC and iSCSI protocols and have no strong opinion about going either way, you probably wonder about trade-offs in terms of cost and performance. Sometimes the cost is roughly the same and you just want more IOPS or throughput for the buck. Other times the cost is not a concern but you’d like to know if the array can deliver more than bare minimum required for your workload.&lt;/p&gt;

&lt;h3 id=&quot;nva-1152-design&quot;&gt;NVA-1152-DESIGN&lt;/h3&gt;

&lt;p&gt;I sometimes work with NetApp E/EF-Series arrays and for a while now I’ve been waiting to find out how well it works with 25 GigE iSCSI, especially since NetApp has added Mellanox Ethernet switches to its solutions. That has finally happened in NetApp NVA-1152-DESIGN. In this document a NetApp HCI server and the EF280 all-flash array were tested to provide an idea of how these building blocks perform and for comparison purposes (NetApp HCI servers don’t come with FC HBAs) FC was tested along iSCSI.&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;https://www.netapp.com/pdf.html?item=/media/21016-nva-1152-design.pdf&quot;&gt;NVA-1152-DESIGN&lt;/a&gt; isn’t long (just the way I like it!), so give it a quick look. It answers precisely that question.&lt;/p&gt;

&lt;p&gt;In summary, iSCSI is &lt;strong&gt;not&lt;/strong&gt; slower. iSCSI did well in both sequential and random performance. And depending on the way you calculate, some folks might say it’s faster. How so? If you already have NetApp HCI for your VI and need to add a 5 GB/s workload to your environment, with iSCSI you don’t have to buy FC switches and can use that money to buy a EF300 or two additional H615C servers, which would get you &lt;em&gt;more performance per dollar spent&lt;/em&gt;. (In my opinion you’d also get better DevOps and automation integrations and one less protocol to deal with, which represents additional savings and/or value.)&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/assets/images/netapp-hci-ef-e-series-iscsi-vs-fc-nva-1152-design.png&quot; alt=&quot;iSCSI vs FC Performance with NetApp HCI H615C and EF280&quot; /&gt;&lt;/p&gt;

&lt;p&gt;In these tests iSCSI was slower in sequential read performance but that’s not unexpected - 2 x 25 GigE has less bandwidth than 4 x 16 Gbps used in the FC test (or 2 x 32 Gbps, if FC used 32 Gbps common these days.) If you need more performance per server with iSCSI, read on!&lt;/p&gt;

&lt;p&gt;In the case you’re not familiar with NetApp HCI compute node models (“H Series servers”) all H615C models come with 2 x 25G and H410C has twice as many 25G NICs (4 x 25G; also 2 x 1/10G but those are usually used for Management), so if a higher sequential performance is desired it is possible to get it with one of the H-Series H410C models.&lt;/p&gt;

&lt;p&gt;If we use one of the H410C models, even if we team (or “bond”) only the 25G interfaces and use the remainder (2 x 1/10G) for management, that’s still 100G of aggregate bandwidth for application &amp;amp; iSCSI workloads. And you can still use those systems in a NetApp HCI environment (you’d need VMware Distributed Switch to aggregate the of 4 x 25G) and avoid creating management islands, which you’d likely end up with if you were to have a HCI system for VI and another cluster for a “high throughput” application. Note that H410C models have several CPU configurations and - generally speaking - Xeon Gold CPUs with more cores tend to deliver better performance than Xeon Silver CPUs with fewer cores.&lt;/p&gt;

&lt;p&gt;When I say “for application &amp;amp; iSCSI workload” I mean your application could consume up 50% of that network bandwidth (when data is written to storage), but it could also use 1% (when data is queried, a 10 byte JSON search query can result in 100 Gbps of iSCSI throughput with H410C &amp;amp; the EF300, for example.)&lt;/p&gt;

&lt;p&gt;Which reminds me: if you need even more performance from iSCSI storage consider the latest mid-range EF model, EF300 (or the E5760, if you want HDDs or hybrid HDD/SSD storage). The E/EF Series &lt;a href=&quot;https://www.netapp.com/data-storage/e-series/&quot;&gt;product page&lt;/a&gt; has more details on all these models (as of now, the datasheets are at the bottom of the page.)&lt;/p&gt;

&lt;h3 id=&quot;scaling-out&quot;&gt;Scaling out&lt;/h3&gt;

&lt;p&gt;Another thing to note is many apps are &lt;strong&gt;not&lt;/strong&gt; limited to the performance of a single H-Series server or E/EF Series array. If the application itself is scalable, you can consider using its native scale-out features (example: Splunk). If it’s not, you can consider using &lt;a href=&quot;https://www.google.com/search?hl=en&amp;amp;q=netapp%20beegfs&quot;&gt;BeeGFS&lt;/a&gt; with multiple servers or disk arrays to get high performance to/from a single namespace.&lt;/p&gt;

&lt;p&gt;Among other things, BeeGFS allows you to get to your data in a scale-out manner. An example for an HCI environment: say we have a small NetApp HCI cluster made with three H615C compute nodes and we need to ingest IoT data at 5 GB/s. If data is coming from different sources, we don’t need to buy a pair of servers each of which (for High Availability) can do 5 GB/s, or even worse (for many other HCI environments) three such servers. Enterprise users would be much better off with a BeeGFS support contract and a fast enough E/EF array (or two slower arrays, as those too can scale out with BeeGFS.)&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;3 x H615C; only 2 x H615C are necessary for 5 GB/s, we can tolerate the loss of one node (N+1)&lt;/li&gt;
  &lt;li&gt;1 x BeeGFS support contract; to let VMs running on HCI compute write to shared file system&lt;/li&gt;
  &lt;li&gt;1 x E/EF array (E5760 or EF570, for example) to share data to BeeGFS VMs&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This approach also makes it easier to scale your workloads. If you need 8 GB/s, just add a server. If you need 20 GB/s, add several servers and one or two additional array.&lt;/p&gt;

&lt;p&gt;Some applications scale out without a shared file system because they can parallelize their workload and do not require shared file system. To scale with those, you’d just add servers or storage as necessary.&lt;/p&gt;

&lt;h3 id=&quot;networking&quot;&gt;Networking&lt;/h3&gt;

&lt;p&gt;Don’t forget to use proper Ethernet switches such as NVIDIA Networking (Mellanox) SN-2010, SN-2100, and SN-2700, to get a network setup that can handle high throughput and low latency workloads, whether it’s &lt;a href=&quot;https://www.youtube.com/watch?v=SCzk3ZpfT-Y&quot;&gt;backup&lt;/a&gt;, analytics, video streaming or any other non-HPC workload that can work with iSCSI.&lt;/p&gt;

&lt;p&gt;All currently available NetApp HCI servers use Mellanox 25 GigE NICs which means less time wasted on reading compatibiltiy and interoperability documentation.&lt;/p&gt;

&lt;h3 id=&quot;hpc&quot;&gt;HPC&lt;/h3&gt;

&lt;p&gt;What about HPC?&lt;/p&gt;

&lt;p&gt;Many HPC workloads can work with iSCSI, but HPC users will always be better off with custom-configured or even custom-built servers and networks that talk Infiniband or NVMe, but scale-out workloads that consume millions of IOPS or tens of GB/s in sequential throughput appear within reach to NetApp HCI users with externally attached E/EF arrays.&lt;/p&gt;

&lt;p&gt;Many enterprises have migrated or want to migrate their workloads to HCI, but high performance workloads can still be challenging, or cost more to run in HCI environments. NetApp HCI’s disaggregated design makes it possible to easily and economically handle such workloads by adding E/EF-Series arrays to your NetApp HCI, while retaining the agility and convenience of HCI.&lt;/p&gt;

&lt;p&gt;It’s rarely justifiable to introduce novel or complex protocols or create management islands. Even if your high-throughput applications run in containers, you can still make use of E/EF-Series (NetApp Trident used by NetApp HCI and ONTAP can also &lt;a href=&quot;https://netapp-trident.readthedocs.io/en/latest/support/requirements.html#supported-backends-storage&quot;&gt;provision iSCSI container storage on E/EF arrays&lt;/a&gt;.)&lt;/p&gt;</content><author><name>scaleoutSean</name></author><summary type="html">If you search for “iSCSI vs FC performance” it’s not easy to find a recent like-for-like comparison from enterprise vendors. I’ve been puzzled by this and I have some theories as to why, but regardless - the fact is there aren’t that many comparisons out there.</summary></entry><entry><title type="html">Set Temporary Storage QoS Policy for SolidFire Volume</title><link href="https://scaleoutsean.github.io/2020/11/28/powershell-set-sfqosexception.html" rel="alternate" type="text/html" title="Set Temporary Storage QoS Policy for SolidFire Volume" /><published>2020-11-28T00:00:00+08:00</published><updated>2020-11-28T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/11/28/powershell-set-sfqosexception</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/11/28/powershell-set-sfqosexception.html">Say we have a Volume ID 321 with a storage QoS Policy ID 5, and need to run a temporary job that would benefit from a different Storage QoS Policy (higher or lower, it doesn't matter).

With PowerShell we'd normally use `Get-SFVolume` to get current QoS settings (could be a dictionary of custom Min, Max and Burst values, or a Storage QoS Policy ID (integer)), store that somewhere and set a new policy (e.g. 7). After we're done we'd restore the previous QoS setting using `Set-SFVolume`.

With `Set-SFQosException` (find the module in my Github repo Awesome SolidFire) we can do this:

```powershell
Set-SFQosException -VolumeID 321 -SFQosPolicyId 7
``` 

First we check the current Volume settings, find that Volume ID 321 uses QoS Policy ID 5, take that value and store it in volume attributes (`&quot;SfQosId&quot;: &quot;5&quot;`).

Then we run our task - batch job, report, backup, QA test... After we're done, we run:

```powershell
Set-SFQosException -VolumeID 321
```

The second time there's no `-SFQosPolicyId` so `Set-SFQosException` simply resets Volume QoS Policy to the value of `SfQosId` we stored earlier (SfQosId K-V pair is not deleted). That's it. 

To make it simple and robust, it comes without the ability to work with Volumes with Custom Storage QoS Policy.

There may be some quirks (especially if you screw around with QoS policy in between the two steps above) but it seems to work.</content><author><name>scaleoutSean</name></author><summary type="html">Say we have a Volume ID 321 with a storage QoS Policy ID 5, and need to run a temporary job that would benefit from a different Storage QoS Policy (higher or lower, it doesn’t matter).</summary></entry><entry><title type="html">SolidFire mNode and HCC Log Fowarding</title><link href="https://scaleoutsean.github.io/2020/11/27/solidfire-mnode-hcc-log-forwarding.html" rel="alternate" type="text/html" title="SolidFire mNode and HCC Log Fowarding" /><published>2020-11-27T00:00:00+08:00</published><updated>2020-11-27T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/11/27/solidfire-mnode-hcc-log-forwarding</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/11/27/solidfire-mnode-hcc-log-forwarding.html">SolidFire Management Node (aka mNode) is a VM that runs Hybrid Cloud Services (various &quot;management stuff&quot;.) Let's say we wanted to collect and analyze those logs.

How could we get the logs out the smart way?

Before we begin, we need to know what we're dealing with. As of today, we have two groups of logs on mNode:

- mNode system logs (the usual Linux stuff)
- HCC service logs (JBOC, just a bunch of containers)

There are no local users so presumably everything that's worthy of our attention is in system and HCC log(s).

Key software components:

- NetApp SolidFire Management Node (mNode) v12.2
  - Docker v18.09.8
  - rsyslogd 8.1904.0
- NetApp Hybrid Cloud Control (HCC) v2.16.66

### mNode

Backup your /etc/rsyslog.conf and hack away! I added these three rows for my $DST server reachable at $DST_PORT (`$DST:$DST_PORT` such as 2.2.2.2:2514):

```raw
*.* action(type=&quot;omfwd&quot; target=&quot;$DST&quot; port=&quot;$DST_PORT&quot; protocol=&quot;tcp&quot;
            action.resumeRetryCount=&quot;100&quot;
            queue.type=&quot;linkedList&quot; queue.size=&quot;10000&quot;)
```

There are fancy features in this module. You can compress the log, you can set a re-try interval (for the 100 tries mentioned above; see `ActionSendTCPRebindInterval` the rsyslog documentation), you can use a specific `device` (which may be useful if you have multiple NICs as uses who use mNode with Persistent Volumes do) and so on. RTFM!

With this set up, we can now monitor OS-level events:

```raw
Nov 27 10:30:21 192.168.1.31 sshd[19710]: Postponed keyboard-interactive for admin from 192.168.1.12 port 58430 ssh2 [preauth]
Nov 27 10:30:23 192.168.1.31 sshd[19710]: Postponed keyboard-interactive/pam for admin from 192.168.1.12 port 58430 ssh2 [preauth]
Nov 27 10:30:23 192.168.1.31 sshd[19710]: Accepted keyboard-interactive/pam for admin from 192.168.1.12 port 58430 ssh2
```

I should note that there are all sorts of logs under /var/log, some of which are likely not captured by syslog, so additional modifications to rsyslog.conf would be required if we wanted to capture those logs.

### HCC

This one seems easy. Backup `/etc/docker/daemon.json` and hack away, right? Wrong!

With log-related additions to daemon.json we can send Docker logs to `$DST:$DST_PORT`, also using TCP like we did with rsyslog.

```json
{
    &quot;bip&quot; : &quot;169.254.100.1/22&quot;,
    &quot;log-driver&quot;: &quot;syslog&quot;,
    &quot;log-opts&quot;: {
        &quot;syslog-address&quot;: &quot;tcp://$DST:$DST_PORT&quot;,
           &quot;tag&quot;: &quot;solidfire&quot;
    }
}
```

The `bip` thing was there before - I only added the log-related entries.

The tag key may be useful for classification and grouping at the destination. I should probably have done something similar for the rsyslog above. With that tag Docker logs (below tagged with `hcc`) are easier to tell apart from mNode (OS) logs (`systemd`), although we could also use syslog at the destination to save each source to a different file:

```raw
Nov 27 10:28:27 192.168.1.31 systemd[1]: Stopping Deploy Element Auth service...
Nov 27 10:28:27 192.168.1.31 systemd[1]: Stopping Login Service...
Nov 27 10:28:27 192.168.1.31 hcc[20264]: killing the spooler with pid 6
```

Once we're done with changes to rsyslog.conf and Docker's daemon.json, we can reboot mNode.

### Wait, what do you mean &quot;reboot&quot;

Yep, I just rebooted mNode to see if my settings worked. What if we wanted to reload rather than reboot? Feel free to JFGI (I'm not too curious how to do that on Docker v18.09.8!)

At the destination syslog server, tail the forwarded log(s) to see a combined log from mNode's rsyslog and Docker:

```raw
Nov 27 08:16:01 192.168.1.31 CROND[30647]: (root) CMD (cd / &amp;&amp; /usr/bin/flock -x /var/lib/sftracing/lock -c 'logrotate --state=/var/lib/sftracing/status /var/lib/sftracing/logrotate.conf' &gt;/dev/null)
Nov 27 08:16:51 192.168.1.31 dockerd[20264]: time=&quot;2020-11-27T08:16:51.520518736Z&quot; level=info msg=&quot;NetworkDB stats mnode(2e34102d1390) - netID:jsgguqh5mxhgjxxf1qkvlvs0t leaving:false netPeers:1 entries:43 Queue qLen:0 netMsg/s:0&quot;
Nov 27 08:16:51 192.168.1.31 dockerd[20264]: time=&quot;2020-11-27T08:16:51.520610196Z&quot; level=info msg=&quot;NetworkDB stats mnode(2e34102d1390) - netID:moxqzm0ca0yon5wgri3yyty94 leaving:false netPeers:1 entries:3 Queue qLen:0 netMsg/s:0&quot;
Nov 27 08:16:51 192.168.1.31 dockerd[20264]: time=&quot;2020-11-27T08:16:51.520624912Z&quot; level=info msg=&quot;NetworkDB stats mnode(2e34102d1390) - netID:lp5fpj7cqik3pqsqsg7o56xsi leaving:false netPeers:1 entries:8 Queue qLen:0 netMsg/s:0&quot;
Nov 27 08:17:01 192.168.1.31 CROND[30752]: (root) CMD (cd / &amp;&amp; /usr/bin/flock -x /var/lib/sftracing/lock -c 'logrotate --state=/var/lib/sftracing/status /var/lib/sftracing/logrotate.conf' &gt;/dev/null)
Nov 27 08:17:06 192.168.1.31 lldpd[21648]: unable to bind to raw socket for interface veth20d4528: No such device
Nov 27 08:17:06 192.168.1.31 lldpd[21651]: unable to initialize veth20d4528
```

System and Docker service log are there, but wait - those docker logs seem very sparse, what's up with that?

#### Logs of Individual Containers

We need to do something about the container logs. What only 30 minutes ago seemed like a trivial exercise is becoming a way to waste an entire evening...

Container logs go to `/var/lib/docker/containers/${Id}/${Id}-json.log` (where ID is the container ID) as expected. These IDs change after each restart (for example, in the case of container image `logs-svc` , we'd get a different ID every time the OS or container are restarted.)

For now the important part is that the information we need is there:

```raw
{&quot;log&quot;:&quot;[pid: 9|app: 0|req: 972/1678] 10.0.0.40 () {32 vars in 1303 bytes} [Fri Nov 27 08:33:13 2020] GET /1/assets/storage-clusters/5a6502a3-8bcb-4617-8cce-64127996545c =\u003e generated 735 bytes in 6 msecs (HTTP/1.1 200) 5 headers in 154 bytes (1 switches on core 0)\n&quot;,&quot;stream&quot;:&quot;stderr&quot;,&quot;time&quot;:&quot;2020-11-27T08:33:13.665314987Z&quot;}
{&quot;log&quot;:&quot;[pid: 9|app: 0|req: 973/1679] 10.0.0.40 () {32 vars in 1303 bytes} [Fri Nov 27 08:33:23 2020] GET /1/assets/storage-clusters/5a6502a3-8bcb-4617-8cce-64127996545c =\u003e generated 735 bytes in 6 msecs (HTTP/1.1 200) 5 headers in 154 bytes (1 switches on core 0)\n&quot;,&quot;stream&quot;:&quot;stderr&quot;,&quot;time&quot;:&quot;2020-11-27T08:33:23.670857855Z&quot;}
```

Next we need to figure out why that stuff isn't being forwarded out. The problem is we may not be able to do much with that information: we can't change HCC's container startup parameters (not in a supported way anyway).

We don't have to be worried about that scenario because we can always get to the container logs via the [HCC API](https://docs.netapp.com/us-en/hci/docs/task_hcc_collectlogs.html#use-the-rest-api-to-collect-logs) which lets us poll the HCC API endpoint and those logs every $N seconds (or get last $M lines from particular container log). That functionality reflects the logs command of Docker:

```sh
sudo docker logs c43bcd1f0431 --since 2020-11-26
```

While we can poll the API for container logs this way, a better approach would be to run an agent that scans /var/lib/docker/containers/ and takes care of everything.

But *why* are the logs not sent together with Docker logs? The answer is under `/etc/rsyslog.d` - in the file named `30-docker.conf`, to be precise:

```raw
:syslogtag, startswith, &quot;docker&quot; /var/log/docker.info
&amp; stop
```

We'd have to hack that file or come up with another file (`40-hcc.conf`, for example) where we'd ask rsyslog to find our logs in `/var/lib/docker/containers/*/*-json.log`. But - alas! - this version of rsyslog doesn't do directory name wildcards so that's out of the question as well.

We could write a new fancy rsyslog rule and dynamically reload rsyslog.conf, but that wouldn't be pain-free either because interesting HCC container logs are all owned by `root:root` which wouldn't work for rsyslog.conf (another can of worms.)

Time to take another look at that HCC API method...

#### HCC `GET /logs`

HCC logs seem to be rotated very slowly (weeks) so it appears quite safe to fetch them every X minutes or hours. We can use the `since` parameter to dynamically get log entries created in last X minutes or hours, and avoid fetching the logs of stopped containers named `logs-svc` (we only want the logs of a running `logs-svc` container).

```sh
curl -X GET &quot;https://${mNode-IP}/mnode/logs?since=2020-11-26T06%3A00%3A00Z&amp;service-name=logs-svc&amp;stopped=false ...
```

That would query mNode for the log of a running `logs-svc` container and return something like this:

```raw
Response body
Download
=========================================================
    mnode_logs-svc.1.nxk8szxmjuuiyo30frxdltcel
=========================================================
[uWSGI] getting INI configuration from uwsgi_app.ini
*** Starting uWSGI 2.0.19.1 (64bit) on [Fri Nov 27 10:57:26 2020] ***
compiled with version: 9.3.0 on 08 October 2020 02:01:58
os: Linux-4.19.37-solidfire7 #1 SMP Wed Jun 24 20:10:57 UTC 2020
nodename: 42d366150c41
machine: x86_64
clock source: unix
detected number of CPU cores: 6
current working directory: /app
detected binary path: /usr/local/bin/uwsgi
```

This particular log isn't structured, but some other services that deal with API calls have structured logs.

Another detail we should consider is if we want to get logs from particular service, or &quot;all&quot;. We should definitively gather logs one by one. If we look at all `Up` containers in the output of `docker ps -a`, our initial list may look like this:

```raw
node-api-gateway
hcc-ui
credential-svc
vcp-sioc
mongo
redis
logs-svc
hci-monitor
mnode-api
mnode-svc-mon-grafana
package-svc
hardware-svc
vcenter-svc
telemetry-service
storage-service
mnode-svc-authz
inventory-service
mnode-svc-mon-prometheus
docker-proxy
mnode-svc-task-monitor
k8sdeployer
mnode-svc-aiq-collector
```

We could now look at the mNode and HCC documentation and decide which logs we're not interested in. For example we may take a look at the logs of `mnode-svc-mon-prometheus` and decide we don't want those. 

With that information we should be able to prune the list to a more manageable length and create a script that loops through a list of container services and every N minutes fetches last (N+1) minutes of service logs.

### Fowarding to Multiple Destinations

For rsyslog I would assume one can simply repeat an rsyslog `action` twice.

For Docker, depending on the version and configuration, `docker logs` may [or may not](https://docs.docker.com/config/containers/logging/dual-logging/) work the way you expect - and that also depends upon your expectations.)

As you can see at that page, Docker *cannot* log to multiple destinations. Oops! It also can't retry like rsyslog can. So it appears for some folks it may be better to log to `local` and have a 3rd party agent (or just rsyslog) gather those logs and send them to multiple destinations.

### What about SolidFire storage cluster logs

Short version: you can set one or more syslog destinations with a simple CLI or API call to MVIP.

See my repo Awesome SolidFire and SolidFire logging-related videos on YouTube.

## Conclusion

mNode system and Docker service logs can be forwarded with ease, but at this time logs of individual containers should be gathered by using the HCC API.

While that is not very inconvenient, we should also investigate 3rd party log forwarding tools that could be deployed inside of mNode VM and eliminate the need to use the API.

If you make any changes on your production system, don't work by these blog posts. Consult the documentation, read the KB articles and check with Support.

### Follow-up (2020/12/05)

As mentioned earlier, it's not supported to add your own containers to mNode VM. Why? It doesn't matter, what matters is it's not supported. It likely has to do with VM capacity and resources, as well as unpredictable interactions with existing container and non-container workloads. But whatever it is, the point is there's no need to create a how-to because the right how-to is &quot;don't do it&quot; so I'll just add some notes on how I made it work in a non-supported manner.

#### Input

Filebeat documentation for Docker isn't great, but (h/t: logz.io) I used `prima/filebeat` and didn't want to spend much time on making it look and work nice. My inputs were all Docker logs:

```yaml
filebeat.prospectors:
 - input_type: log
  paths:
    - /var/lib/docker/containers/*/*-json.log
  ...
```

The above, once it starts churning through logs, can eat up 2 CPU cores *easily*. Similarly for RAM and disk space. In other words, if you don't incrase mNode VM resources or restrict this container's resources, it can easily take down your mNode.

What if you want to collect just some, rather than all, logs? The list of containers is in this post and what they do is in the documentation. Create multiple filebeat containers or create multiple definitions (YAML format), one per each individual container, and load them like so:

```yaml
filebeat.config.inputs:
  enabled: true
  path: hcc.d/*.yml
```

You'd probably have to read the docs to figure the above out.

After a bit of googling and RTFM I also managed to capture individual container logs (hcc-ui, in this example) with this:

```yaml
filebeat.autodiscover:
  providers:
    - type: docker
      templates:
        - condition:
            contains:
              docker.container.image: hcc-ui
          config:
            - type: container
              paths:
                - /var/lib/docker/containers/${data.docker.container.id}/*.log
              exclude_lines: [&quot;^\\s+[\\-`('.|_]&quot;]
```

#### Output

Filebeat can send container logs to external recepients.

```yaml
output.elasticsearch:
  hosts: [&quot;192.168.1.146:9200&quot;]
```

If you want to send it to two destinations, run two filebeat containers with identical configuration.

Or, if you wanted to store logs locally, add this file to be processed by rsyslog, and leave the forwarding to multiple destinations to rsyslog:

```yaml
output.file:
  path: &quot;/var/logs/docker/filebeat/&quot;
  filename: filebeat
  rotate_every_kb: 1000
  number_of_files: 7
  permissions: 0644
```

In this example retain only 7 x 1 MiB files. That's probably not what you want to do.

#### Result

You'll get some extra junk you may not want, but Filebeat has options and processors that let you strip that, and so does rsyslog. By the looks of it (see the values like `jit` in this JSON file) you may want to ensure that logs are either stripped of confidential info at the source, or sent to destination over encrypted connection.

```json
{
	&quot;@timestamp&quot;: &quot;2020-12-04T17:06:02.511Z&quot;,
	&quot;@metadata&quot;: {
		&quot;beat&quot;: &quot;filebeat&quot;,
		&quot;type&quot;: &quot;doc&quot;,
		&quot;version&quot;: &quot;6.2.3&quot;
	},
	&quot;source&quot;: &quot;/var/lib/docker/containers/f14f10d6aefccf3be511c259a79e1cdae46f6f1da24479704392e3852cd45aa8/f14f10d6aefccf3be511c259a79e1cdae46f6f1da24479704392e3852cd45aa8-json.log&quot;,
	&quot;offset&quot;: 73282162,
	&quot;log&quot;: &quot;2020-11-28T23:49:25.683922Z |     6 | 132721654 |    MainProcess-uWSGIWorker1Core0  |         flask_authtools:117  | DEBUG   | Authenticated user with claims: {'nbf': 1606607365, 'exp': 1606607485, 'iss': 'https://192.168.1.30/auth', 'aud': ['mnode_api', 'mnode_credential'], 'client_id': 'hci-monitor', 'iat': 1606607365, 'jti': 'YECZS3fkjxfpPx38gofKlg', 'scope': ['mnode_api:r', 'mnode_credential:r']}\n&quot;,
	&quot;stream&quot;: &quot;stderr&quot;,
	&quot;time&quot;: &quot;2020-11-28T23:49:25.684342722Z&quot;,
	&quot;beat&quot;: {
		&quot;hostname&quot;: &quot;59e98457dadf&quot;,
		&quot;version&quot;: &quot;6.2.3&quot;,
		&quot;name&quot;: &quot;59e98457dadf&quot;
	}
}
```</content><author><name>scaleoutSean</name></author><summary type="html">SolidFire Management Node (aka mNode) is a VM that runs Hybrid Cloud Services (various “management stuff”.) Let’s say we wanted to collect and analyze those logs.</summary></entry><entry><title type="html">SolidFire Provider for Terraform</title><link href="https://scaleoutsean.github.io/2020/11/26/solidfire-terraform-provider.html" rel="alternate" type="text/html" title="SolidFire Provider for Terraform" /><published>2020-11-26T00:00:00+08:00</published><updated>2020-11-26T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/11/26/solidfire-terraform-provider</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/11/26/solidfire-terraform-provider.html">The Provider is now available from the official Terraform registry. This means we no longer need to download and build it manually. It is also easy to use it from Terraform scripts because it is deployed automatically like other providers available from the registry.

For now it can manage (create, delete) essential resources:

- storage account
- IQN
- Volume Access Group (VAG)
- volume

If you'd like it to be able to do more, please provide feedback to #netapp-hci (NetApp Pub Slack) or on Github.

There's a short (1 minute) [demo](https://www.youtube.com/watch?v=pyU68JqPuMM) based on the Github example below, if you'd like to see it in action.

### Resources

- [SolidFire Provider](https://registry.terraform.io/providers/NetApp/netapp-elementsw/latest) at the Terraform Registry Web site
- Source code, releases and examples can be found [here](https://github.com/NetApp/terraform-provider-netapp-elementsw/releases/tag/v20.11.0)</content><author><name>scaleoutSean</name></author><summary type="html">The Provider is now available from the official Terraform registry. This means we no longer need to download and build it manually. It is also easy to use it from Terraform scripts because it is deployed automatically like other providers available from the registry.</summary></entry><entry><title type="html">Helpful waste of time</title><link href="https://scaleoutsean.github.io/2020/11/24/scary-bs-postman-ssl-certs.html" rel="alternate" type="text/html" title="Helpful waste of time" /><published>2020-11-24T00:00:00+08:00</published><updated>2020-11-24T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/11/24/scary-bs-postman-ssl-certs</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/11/24/scary-bs-postman-ssl-certs.html">When dealing with input that consists of unweildy TLS certificates Postman can helpfully hint at potential JSON validity problems and save you time. I'm sure some other tools are like that as well.

![Scary Bullshit](/assets/images/postman-scary-bs-setsslcertificate.png)

With such help, instead of trying like an idiot you can fix your inputs before that, get things done and have a nice rest of the day.

All right, let's try to remove all line breaks until Postman stops hinting.

After having removed 10 lines, I start thinking &quot;oh man, this is slow! Let's automate!&quot;

Try sed, get stuck, Google it, done!

Back to Postman. No hints. Send. Yikes!!! Invalid cert!

Aha! Back to checking if my cert is in the right format. DER. PEM, etc. What a hassle!

All right, the certs are fine as I had thought. I see - I should replace line breaks with `\n`'s, says a helpful Stack Overflow answer.

More sed. Done! Damn, it still doesn't work!

Let's recheck those certs...

Eventually I got desperate and tried to work by [my own notes](https://github.com/scaleoutsean/awesome-solidfire/blob/master/encryption/kmip-thales-keysecure.md) from Github which led me to believe I should simply paste that junk as-is and hit Send. It worked.

I don't blame Postman, those visual hints probably saved me more than 30 minutes over the years. 

But why things have to be so complicated?</content><author><name>scaleoutSean</name></author><summary type="html">When dealing with input that consists of unweildy TLS certificates Postman can helpfully hint at potential JSON validity problems and save you time. I’m sure some other tools are like that as well.</summary></entry><entry><title type="html">Deploying Rancher Worker VMs to NetApp HCI with VMware VSS</title><link href="https://scaleoutsean.github.io/2020/11/20/rancher-and-drs.html" rel="alternate" type="text/html" title="Deploying Rancher Worker VMs to NetApp HCI with VMware VSS" /><published>2020-11-20T00:00:00+08:00</published><updated>2020-11-20T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/11/20/rancher-and-drs</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/11/20/rancher-and-drs.html">Earlier this week I spotted the advice to not select a VM host when deploying Rancher worker VMs to a vSphere environment where DRS is enabled, because &quot;DRS is smarter than you.&quot;

![Select VM Host in a DRS environment](/assets/images/rancher-2-deploy-to-specific-vsphere-host.png)

That's almost certainly true - especially in the mid and long term - but at the moment of deployment you might have an edge.

Situation:

- Five VM hosts with ESXi
- Virtual Standard Switch (VSS) because maybe you don't have vSphere Enterprise Edition
- Rancher VMs are commonly deployed on VM Hosts 1, 2, and 3 so that they don't run all over the place

If you use a Resource Pool and the three hosts are part of it, then it's more likely that DRS will make better decisions over time.

If you do not have a Resource Pool and only Hosts 1, 2 and 3 are connected to networks required by your Rancher workloads, it's probably better to pick one of the hosts and manually redistribute VMs after that (add anti-affinity rules, etc.).

One such situation is where only Hosts 1-3 have networks or allow VLANs that can allow Rancher VMs to get to SolidFire Storage Virtual IP (i.e. iSCSI). 

In a regular NetApp HCI environment only hosts (not VMs) need to get to SolidFire iSCSI IPs (each ESXi has 2 networks for that purpose). With VDS this is easy to apply and maintain over a cluster of hosts, but with VSS it takes some effort and some users may find value in limiting that complexity to only a subset of VM hosts.

If you use VSS and run Rancher on a subset of VM hosts, it's a good idea to create a Resource Pool with these &quot;Rancher&quot; Hosts.</content><author><name>scaleoutSean</name></author><summary type="html">Earlier this week I spotted the advice to not select a VM host when deploying Rancher worker VMs to a vSphere environment where DRS is enabled, because “DRS is smarter than you.”</summary></entry><entry><title type="html">Dark Mode for NetApp HCI and SolidFire</title><link href="https://scaleoutsean.github.io/2020/11/20/solidfire-netapp-hci-dark-mode.html" rel="alternate" type="text/html" title="Dark Mode for NetApp HCI and SolidFire" /><published>2020-11-20T00:00:00+08:00</published><updated>2020-11-20T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/11/20/solidfire-netapp-hci-dark-mode</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/11/20/solidfire-netapp-hci-dark-mode.html">### NetApp HCI, SolidFire, eSDS Web UI

![SolidFire Web UI in Dark Mode](/assets/images/solidfire-netapp-hci-dark-mode.png)

The above image is SolidFire v12.0 viewed from Firefox and Dark Reader.

There's another open source browser extension called Dark Mode. 

Both are available for Firefox, Chrome, and Edge.

### Element Plug-in for VMware vCenter (aka VCP)

It too works well (evaluated with Dark Mode, for example.)

If you use Dark Theme in vCenter and want VCP in dark mode, you need to switch back to Light Theme in vCenter, and then enable dark mode in your browser extension.

### NetApp HCC

NetApp HCC (Hybrid Cloud Control) also looks nice with dark mode extensions.

### Make sure you get the extension you want

Check the home page of the extension, reviews, etc. 

Knock-off extensions may attempt to steal your credentials.</content><author><name>scaleoutSean</name></author><summary type="html">NetApp HCI, SolidFire, eSDS Web UI</summary></entry><entry><title type="html">Muddying the gh-pages waters</title><link href="https://scaleoutsean.github.io/2020/11/17/muddying-gh-page-waters.html" rel="alternate" type="text/html" title="Muddying the gh-pages waters" /><published>2020-11-17T00:00:00+08:00</published><updated>2020-11-17T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/11/17/muddying-gh-page-waters</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/11/17/muddying-gh-page-waters.html">I've done my fair share of googling and was surprised to see there are so many complex ways to use Github pages.

As a person supposed to solve problems, I can't say I like that there are so many ways to serve static pages and at the same time I've found the help pages to be confusing, so I'll just try to explain two key points in the hope to help few of you stop banging their head against the wall.

## Two key points for individual Github accounts

- There are *account* sites (one per individual account). These can be hosted at https://$ACCOUNT.github.io (with or without a `/blog` or `/docs` at the end - this is entirely up to you). My account name is scaleoutsean, for example.
- There are *project* (repo) pages (e.g. `solid-rancher`). These can only be hosted at https://$ACCOUNT.github.io/$PROJECT (yet further followed - or not - by custom paths such as `/rtfm`, for example)

It took me hours of reading TFM (Github Pages, Jekyll). Googling and SO-ing to stumble across this information.

If you've followed until here, you can stop and go back to your troubleshooting - if this hasn't helped you, what follows won't either.

If you were to maintain static Web content this way, you'd keep your Jekyll site in `/docs` and Github would convert them to static pages for you. This does *not* mean you'd end up with site URLs such as (in the case of $ACCOUNT named `blah-blah`) https://blah-blah.github.com/docs. The `/docs` directory in your repo is just a &quot;pick up location&quot; for Github Pages to process your Jekyll content. You decide - in your static Web site generator settings - whether you use a subfolder or not.

If you have locally generated static or non-Jekyll content to begin with, you'd just copy it to this folder /docs and Github would take it from there (and in fact you'd want that content to be left alone because it's ready for publishing.)

## To git or not to git

Another annoying thing is that the choice of branch and directory seems important. Actually it doesn't matter *unless* you plan to have Github help you generate content (static Web pages.) 

The directory is simply a place which Github Pages watches. You may keep your Jekyll site source in `/junk`, have it generate static content in `/junk/_site` and then copy that subdirectory contents to `/data`. That will work. 

Or you could tell Jekyll to output static content directly to `/data`. 

Or you could just keep Jekyll site in `/data` and let Github Pages process it for you server-side after every push.

Which way is better? It depends.

In my case, I decided against the last, Github-integrated approach because I don't have collaborators for these repos and I can't think of any other reason why I should use the automated workflow. Surely it can be a very convenient and even necessary for some repos, but this process also has its limitations so everyone must decide for themselves (and that's why we have half a dozen different ways of going from A to B.)

For example, the workflow approach doesn't support arbitrary Jekyll versions and plugins - you can use only those plugins which Github supports. If you want to add an unsupported plugin - oops, you won't be able to. You also need to implement that auth token thing (yet more security procedures), you need to watch Github to see if your builds completed or not, and there's more.

## Yet another way to deploy

I'm sure you're like me and have read about half a dozen ways to publish static content to Github Pages, but here's what works for me (I already don't like some things about it, but I don't like it less than all the other ways):

- The repo used for my account (github.com/scaleoutsean/scaleoutsean), is set to publish Github pages from `/docs` on the `master` branch. The blog content itself doesn't use any `baseurl` (as you can see from the URL of this post)
- My project github.com/scaleoutsean/solid-rancher is also set to publish Github pages from '/docs' on `master`. This project uses `baseurl=/solid-rancher` so that `/about` from this &quot;sub-site&quot; eventually becomes https://scaleoutsesan.github.io/solid-rancher/about)
- For each of these sites, I use a separate (but similar) procedure to generate static content and make a copy to `/docs` (on the master branch) and place a file `.nojekyll` under `/docs` to tell Github Pages that the content is already static and doesn't need to be processed
- Finally for each of the repos I run `git add docs/`, commit and push to Github

As you can see there's no `gh-pages` branch, git subtree split or other wizard-level commands. I don't use Git every day and I'd rather not use complicated git commands even if it's just in a script.

One of the downsides of my approach is I can't have a project called `categories` and publish its \docs (not without using a different `baseurl` path because scaleoutsean.github.io/categories is now taken by the blog itself; or I could change the blog's top level link to &quot;category&quot; to free up that URL path). I also must maintain my own process (the good and bad thing about it: only I can break and fix it.)

I wouldn't recommend this approach, but I find it less worse for my purpose than other approaches I've seen.

## I used be a README.md kind of guy

One Github project I inherited used to have a static Web site. At the time I immediately nuked that static site directory to spare myself from the inevitable waste of time resulting from the need to lear yet another of the many good-to-know-but-not-essential skils. In hindsight it was the right decision.

As I've just completed publishing docs from a new repo (`solid-rancher`) for which I could afford to give this a try, for now I think static Web site docs are nicer to look at compared to README.md. Instead of having everything in one gigantic README.md, I have sections. It is also easier to search: CTRL+F works fine on long READ.me, but requires a lot of scrolling up and down if the page.)

But there's a price to pay for all these nice things: it's more time consuming to make updates, I have created and now have to maintain a build process, and my docs now have YAML headings (which would require some scripting in order to migrate back to pure markedownif I were to change my mind), so I'm still undecided on whether I really need Github Pages for my repositories.

This isn't to say nobody needs them - my use case is very simple: these aren't code repositories with complex software documentation maintained by half a dozen people. But I'll wait and see rather than move everything to static Web sites. As of now, I still like my README files in the markdown format.</content><author><name>scaleoutSean</name></author><summary type="html">I’ve done my fair share of googling and was surprised to see there are so many complex ways to use Github pages.</summary></entry><entry><title type="html">solid-rancher</title><link href="https://scaleoutsean.github.io/2020/11/16/solid-rancher.html" rel="alternate" type="text/html" title="solid-rancher" /><published>2020-11-16T00:00:00+08:00</published><updated>2020-11-16T00:00:00+08:00</updated><id>https://scaleoutsean.github.io/2020/11/16/solid-rancher</id><content type="html" xml:base="https://scaleoutsean.github.io/2020/11/16/solid-rancher.html">`solid-rancher` it's a new Github repo I created to organize and enrich information related to Rancher on SolidFire (and NetApp HCI in general). 

A non-mission is to substitute or duplicate information from the official documentation (whether from Rancher or from NetApp).

Like with other repos I maintain, the idea is to share useful information quickly and in an acessible way. Quality contributions are welcome.

Edit: this repo is experimentally published as Web site at [https://scaleoutsean.github.io/solid-rancher](https://scaleoutsean.github.io/solid-rancher/) - if you're interested please visit this site instead of the Github repository.</content><author><name>scaleoutSean</name></author><summary type="html">solid-rancher it’s a new Github repo I created to organize and enrich information related to Rancher on SolidFire (and NetApp HCI in general).</summary></entry></feed>